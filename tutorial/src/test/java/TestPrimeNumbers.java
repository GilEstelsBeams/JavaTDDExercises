import org.junit.Test;
import static org.junit.Assert.assertEquals;

public class TestPrimeNumbers {


    @Test
    public void testIsPrimeNumber() {
        boolean isPrimeNumber = true;
        assertEquals(isPrimeNumber,true);
    }
}